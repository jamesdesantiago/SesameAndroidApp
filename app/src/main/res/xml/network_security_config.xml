<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <base-config cleartextTrafficPermitted="false"> <!-- Set to false -->
        <trust-anchors>
            <!-- Trust system certificates for HTTPS -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- Add domain-specific configs below if needed for production HTTPS exceptions -->
</network-security-config>